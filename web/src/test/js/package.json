{
  "name": "airbridge-browser-tests",
  "version": "1.0.0",
  "description": "JavaScript unit tests for AirBridge browser code",
  "scripts": {
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
